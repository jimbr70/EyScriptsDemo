---
# This playbook uses the win_ping module to test connectivity to Windows hosts
- name: Ping 
  hosts: all 

  tasks:
  - name: ping
    win_ping:

  - name: Pausing  3000s  while deploy sccm runs a bit  NO SMARTS YET
    pause:
      seconds: 3000
      prompt: no

